import { createAction } from '@ngrx/store';

export const hideBackButton = createAction('[hide top-menu button] hideBackButton');
export const showBackButton = createAction('[show top-menu button] showBackButton');
